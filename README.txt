 -- This zip file contains the code for the ITEC327 project based on the requirements given. --

 -- File Details --
 
   --main.php: Main page of the to-do list.
   --server.php: Request handling for creating, modifying, deleting, searching, etc.
   --create.js: Page behaviour during task creation.
   --delete.js: Page behaviour during task deletion.
   --modify.js: Page behaviour during task modification.
   --sort.js: 	Page behaviour during sorting.
   --style.css: Page styling and embellishments
   --.vscode: 	launch.json file for vscode (only if using vscode and xampp)

 -- Instructions -- 

    -- Import "todolist.sql" for database, tables, and user to be created.
    -- Run "main.php".